apiVersion: v1
kind: Service
metadata:
  name: aers-frontend-service
  namespace: aers
spec:
  selector:
    app: aers-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
